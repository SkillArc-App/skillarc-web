export const userCanSeeJobs = (user: any) => {
  return true
  // return (
  //   user?.profile &&
  //   user.profile.profileSkills &&
  //   user.profile.profileSkills.length > 0 &&
  //   user.profile.stories &&
  //   user.profile.stories.length > 0
  // )
}
