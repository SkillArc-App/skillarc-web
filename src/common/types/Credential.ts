export type Credential = {
  id: string
  organizationId?: string
  name?: string
  profileId?: string
  issueDate?: string
  description?: string
  // profile?: Profile
  // organization: Organization
}
